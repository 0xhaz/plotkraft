{
  "firestore": { "rules": "firestore.rules", "indexes": "firestore.indexes.json" },
  "database": { "rules": "database.rules.json" },
  "storage": { "rules": "storage.rules" },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8181 },
    "database": { "port": 9001 },
    "storage": { "port": 9199 },
    "ui": { "enabled": true, "port": 4001 },
    "hub": { "port": 4401 },
    "_comment": "singleProjectMode is off: it makes the rules tests share a datastore with local dev data, so clearFirestore() in the test suite wipes the project you are working on.",
    "singleProjectMode": false
  }
}
